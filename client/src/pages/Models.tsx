import { PublicNav } from "@/components/PublicNav";
import { TokenForgeGlyph } from "@/components/TokenForgeGlyph";
import { ProviderMark } from "@/components/ProviderMark";
import { Badge } from "@/components/ui/badge";
import { formatUsdPerMillion, TOKENFORGE_MODELS } from "@/lib/modelCatalogue";
import { ArrowRight, Braces, ExternalLink, Gauge } from "lucide-react";
import { Link } from "wouter";
import "./models.css";

const FEATURED_VISUALS: Record<string, { source: string; label: string; visual: string; alt: string }> = {
  "glm-5.2": { source: "https://z.ai/blog/glm-5.2", label: "Publisher visual · Z.AI", visual: "/manus-storage/glm-5-2-zai-reference_bfc9a0e7.png", alt: "Publisher-provided GLM-5.2 benchmark figure from Z.AI." },
  "grok-4.5": { source: "https://x.ai/news/grok-4-5", label: "Publisher visual · xAI", visual: "/manus-storage/grok-4-5-xai-reference_a284fce8.png", alt: "Publisher-provided visual for Grok 4.5 from xAI." },
};

export default function Models() {
  return <div className="public-page tf-public-surface models-page"><PublicNav /><main className="catalogue-page"><div className="page-eyebrow"><span className="status-dot" /> Verified text catalogue</div><div className="catalogue-heading"><div><h1>{TOKENFORGE_MODELS.length} text models.<br /><em>One considered interface.</em></h1><p>Every active route supports OpenAI-compatible chat completions, provider-isolated execution, and an exact first-party token rate. Image, audio, embedding, transcription, and research-only routes are intentionally excluded.</p></div><div className="catalogue-stats"><div><strong>{String(TOKENFORGE_MODELS.length).padStart(2, "0")}</strong><span>verified text routes</span></div><div><strong>01</strong><span>consistent API surface</span></div></div></div><section className="model-detail-grid" aria-label="Available text-chat models">{TOKENFORGE_MODELS.map(model => { const visual = FEATURED_VISUALS[model.id]; return <article key={model.id} className={`model-detail-card model-detail-card--${model.tone}`}><div className="model-provider-line"><span className={`model-provider-glyph model-provider-glyph--${model.tone}`}><ProviderMark provider={model.provider} fallback={model.providerMark} size={18} /></span><span>{model.provider}</span><Badge variant="secondary">Available in beta</Badge></div>{visual ? <figure className="model-source-visual"><img src={visual.visual} alt={visual.alt} /><figcaption><a href={visual.source} target="_blank" rel="noreferrer">{visual.label}</a></figcaption></figure> : null}<p className="model-eyebrow">{model.eyebrow}</p><h2>{model.name}</h2><p className="model-description">{model.description}</p><div className="capability-row">{model.capabilities.map(capability => <span key={capability}>{capability}</span>)}</div><section className="model-rate-card" aria-label={`${model.name} TokenForge credit rate`}><div className="model-rate-card__head"><p>TokenForge credit rate</p><span>per 1M tokens</span></div><dl><div><dt>Input</dt><dd>{formatUsdPerMillion(model.inputUsdPerMillion)}</dd><small>per 1M tokens</small></div><div><dt>Output</dt><dd>{formatUsdPerMillion(model.outputUsdPerMillion)}</dd><small>per 1M tokens</small></div></dl><p className="model-rate-note">Applied only to successful requests, using provider-reported input and output token counts.</p></section><div className="model-card-footer"><code>{model.id}</code><a href={model.pricingUrl} target="_blank" rel="noreferrer">{model.pricingSource} <ExternalLink size={13} /></a></div></article>; })}</section><section className="catalogue-note"><div className="catalogue-note__icon"><Gauge size={20} /></div><div><strong>Verified rates are a requirement for activation.</strong><p>Prices link to first-party provider materials and are mapped only where TokenForge can represent the listed input and output token price exactly. Availability is checked when a request begins; TokenForge never silently switches a selected model.</p></div><Link href="/signup" className="models-cta">Create an account <Braces size={16} /></Link></section></main><PublicFooter /></div>;
}

export function PublicFooter() {
  return <footer className="public-footer"><div><span className="footer-brand"><TokenForgeGlyph className="public-footer__glyph" />Token<span>Forge</span></span><p>A considered AI gateway for serious building.</p></div><div className="footer-links"><a href="/models">Models</a><a href="/pricing">Pricing</a><a href="/docs">Documentation</a><a href="/legal/terms">Terms</a><a href="/legal/privacy">Privacy</a><a href="/legal/acceptable-use">Acceptable use</a></div><small>© 2026 TokenForge · <a href="/signin">Sign in</a></small></footer>;
}
